.claude/
.internal/

# Nooit een sleutel of een lokaal omgevingsbestand in de repo. De Google-sleutel hoort
# buiten deze map te staan; zie operations/google-agenda-koppeling.md.
.env
.env.*
*service-account*.json
*-key.json
*credentials*.json

# Lokale testgegevens en het lokale ondertekengeheim van de beheeromgeving.
# Alles wat met .local-data begint: ook reservekopieen zoals .local-data.oud-2210.
# Op 6 september 2026 belandde zo'n map per ongeluk in een commit via `git add -A`.
.local-data*
